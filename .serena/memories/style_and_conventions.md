Code style observed so far: TypeScript with ES module syntax and double quotes in frontend config/CSS files. Next.js App Router structure under `frontend/src/app`. CSS uses Tailwind CSS v4 imports and CSS custom properties in `globals.css`. Project docs emphasize keeping implementation aligned with `doc/` specs and updating docs with implementation changes. Final permission enforcement belongs in the API; frontend permission handling is UX-only.