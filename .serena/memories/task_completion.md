When finishing a frontend task, at minimum run the relevant verification command from `frontend/` such as `npm run build` if possible. Check whether lockfile/workspace-root warnings from Next.js affect module resolution because this repo currently has both `/Users/0rchid3/dev/ashiato/package-lock.json` and `frontend/package-lock.json`. There are no dedicated lint/test scripts in frontend package.json yet, so verification may rely on build/dev checks.